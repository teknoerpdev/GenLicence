export declare class MemoryObjectStore {
    data: any[];
    constructor(data: any[]);
    get indexNames(): {
        contains(columnName: string): boolean;
    };
    index(column: string): {
        openCursor: (keyRange?: IDBKeyRange) => {
            onsuccess: Function;
            onerror: Function;
        };
    };
}
