import { Base } from "@executors/base";
import { IDBUtil } from "@/worker/idbutil";
export declare class Clear extends Base {
    constructor(tableName: string, util: IDBUtil);
    execute(beforeExecute: () => Promise<any>): Promise<void>;
}
