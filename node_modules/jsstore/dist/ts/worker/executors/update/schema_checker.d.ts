import { LogHelper } from "@worker/utils";
import { TableMeta } from "@worker/model";
export declare class SchemaChecker {
    table: TableMeta;
    constructor(table: TableMeta);
    check(setValue: object, tblName: string): LogHelper;
    private checkByColumn_;
}
